{
  "name": "base-ready-template",
  "version": "1.0.0",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy:base": "npx hardhat run scripts/deploy-base.js --network base",
    "deploy:sepolia": "npx hardhat run scripts/deploy-base.js --network baseSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.0",
    "hardhat": "^2.22.0"
  }
}
